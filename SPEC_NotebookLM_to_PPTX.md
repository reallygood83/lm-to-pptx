# NotebookLM PDF to PPTX Converter

## Project Specification Document (v0.4)

**최종 수정일**: 2025-12-25
**상태**: 개발 준비 단계 (GitHub 배포 + Neo-brutalism UI)
**개발자**: 배움의 달인

---

## 1. 프로젝트 개요

### 1.1 문제 정의
Google NotebookLM은 AI 기반 연구 도구로, 슬라이드 생성 기능을 제공합니다. 하지만 생성된 슬라이드는 **PDF 형식**으로만 내보내기가 가능하여 다음과 같은 제약이 있습니다:

- PowerPoint에서 편집 불가
- 개별 슬라이드 수정/재배열 불가
- 디자인 커스터마이징 제한
- **발표자 노트 없음** ← 핵심 Pain Point
- 애니메이션/전환 효과 적용 불가

### 1.2 솔루션 목표
NotebookLM에서 생성된 PDF 슬라이드를 **AI가 분석하여 발표자 노트를 자동 생성**하고, **편집 가능한 .pptx 파일**로 변환하는 도구 개발

### 1.3 핵심 가치 제안
- **🤖 AI 발표자 노트 자동 생성**: 각 슬라이드 이미지를 AI가 분석하여 발표자 노트 작성
- **📚 컨텍스트 자료 기반 노트 강화**: 관련 자료(PDF, TXT, MD)를 첨부하여 더욱 정확하고 풍부한 노트 생성
- **시간 절약**: NotebookLM의 AI 슬라이드 생성 + AI 발표자 노트 + PPT 편집의 유연성
- **워크플로우 개선**: AI 초안 → AI 노트 생성 → 수동 정제 → 최종 발표
- **접근성**: 비기술 사용자도 쉽게 사용 가능

### 1.4 핵심 차별화 요소 ⭐
> **"단순 PDF→PPT 변환이 아닌, AI가 각 슬라이드를 이해하고 발표자를 위한 스크립트를 자동 생성"**

기존 도구들은 단순 포맷 변환만 제공하지만, 본 프로젝트는:
1. AI가 슬라이드 이미지의 내용을 분석
2. 각 슬라이드별 발표자 노트(스피커 노트) 자동 생성
3. 발표 준비 시간 획기적 단축

---

## 2. 기술적 분석

### 2.1 NotebookLM PDF vs PowerPoint 크기 분석 ⭐ NEW

#### 사이즈 비교
| 항목 | NotebookLM PDF | PowerPoint 16:9 | 차이 |
|------|----------------|-----------------|------|
| **픽셀 크기** | 1376 × 768 px | 1920 × 1080 px | +39.5% (가로) |
| **가로세로 비율** | 1.7917:1 | 1.7778:1 | **0.78% 차이** |
| **비율 표현** | ~16.1:9 | 16:9 정확 | 거의 동일 |
| **인치 크기** | - | 13.333 × 7.5 inch | PPT 표준 |

#### 크기 차이 해결 전략
```
┌─────────────────────────────────────────────────────────┐
│  비율 차이: 0.78% → 인간 눈으로 구분 불가                    │
│                                                           │
│  권장 방식: STRETCH (늘리기)                               │
│  ├─→ 원본 비율: 1.7917:1                                   │
│  ├─→ 목표 비율: 1.7778:1 (PPT 16:9)                       │
│  └─→ 결과: 가로 0.78% 압축, 시각적 영향 없음               │
│                                                           │
│  대안 방식들:                                               │
│  • LETTERBOX: 상하 검은 여백 (권장하지 않음)               │
│  • CROP: 좌우 약간 잘림 (정보 손실)                        │
└─────────────────────────────────────────────────────────┘
```

**구현 코드:**
```python
from pptx.util import Inches

# PowerPoint 16:9 표준 크기
SLIDE_WIDTH = Inches(13.333)   # 1920px @ 144 DPI
SLIDE_HEIGHT = Inches(7.5)     # 1080px @ 144 DPI

# 이미지 삽입 시 전체 슬라이드 크기로 STRETCH
slide.shapes.add_picture(
    image_path,
    left=Inches(0),
    top=Inches(0),
    width=SLIDE_WIDTH,    # 늘려서 맞춤
    height=SLIDE_HEIGHT   # 늘려서 맞춤
)
```

### 2.2 NotebookLM PDF 슬라이드 특성 (샘플 분석 결과)

| 항목 | 분석 결과 |
|------|----------|
| 페이지 수 | 10페이지 (가변) |
| 파일 크기 | 8.1MB (이미지 포함) |
| **정확한 크기** | 1376 × 768 픽셀 |
| **정확한 비율** | 1.7917:1 (~16.1:9) |
| 렌더링 방식 | **이미지 기반** (텍스트가 이미지로 래스터화) |
| 워터마크 | 우측 하단 "NotebookLM" 로고 |
| 언어 지원 | 한국어, 영어 등 다국어 |
| 디자인 요소 | 배경색, 아이콘, 표, 차트, 일러스트레이션 |

### 2.3 권장 MVP 접근법

**Phase 1 (MVP)**: 이미지 삽입 + AI 발표자 노트 생성
```
1. PDF 각 페이지를 고해상도 이미지로 추출
2. AI (Gemini/GPT/Claude/Grok)가 각 이미지 분석
3. 슬라이드별 발표자 노트 자동 생성
4. PPT 슬라이드에 이미지 STRETCH 삽입 + 노트 삽입
5. .pptx 파일 다운로드
```

---

## 3. 배포 방식: GitHub 로컬 프로그램 ⭐ NEW

### 3.1 배포 전략

```
┌─────────────────────────────────────────────────────────┐
│  📦 GitHub 배포 방식                                      │
│                                                           │
│  Repository: github.com/[username]/notebooklm-to-pptx    │
│                                                           │
│  사용자 설치 방법:                                         │
│  $ git clone https://github.com/[username]/notebooklm-to-pptx
│  $ cd notebooklm-to-pptx                                  │
│  $ pip install -r requirements.txt                       │
│  $ python main.py                                         │
│                                                           │
│  또는 pip 직접 설치 (향후):                                │
│  $ pip install notebooklm-to-pptx                        │
└─────────────────────────────────────────────────────────┘
```

### 3.2 프로젝트 구조

```
notebooklm-to-pptx/
├── README.md                 # 설치 가이드, 사용법
├── requirements.txt          # Python 의존성
├── config.yaml              # API 키 설정 템플릿
├── config.yaml.example      # 예시 설정 파일
│
├── src/
│   ├── __init__.py
│   ├── main.py              # 메인 진입점
│   ├── converter.py         # PDF → PPTX 변환 핵심 로직
│   ├── ai_providers/        # 멀티 AI API 지원
│   │   ├── __init__.py
│   │   ├── base.py          # 추상 기본 클래스
│   │   ├── gemini.py        # Google Gemini API
│   │   ├── openai.py        # OpenAI GPT API
│   │   ├── anthropic.py     # Anthropic Claude API
│   │   └── grok.py          # xAI Grok API
│   └── ui/
│       ├── __init__.py
│       └── app.py           # Neo-brutalism GUI
│
├── assets/
│   ├── fonts/               # 커스텀 폰트
│   ├── icons/               # UI 아이콘
│   └── styles/              # CSS/스타일 파일
│
├── tests/
│   ├── test_converter.py
│   └── sample_pdfs/         # 테스트용 샘플 PDF
│
└── docs/
    ├── INSTALLATION.md      # 상세 설치 가이드
    ├── API_SETUP.md         # API 키 설정 가이드
    └── USAGE.md             # 사용법
```

### 3.3 시스템 요구사항

```yaml
Python: 3.9+
OS: Windows 10+, macOS 10.15+, Ubuntu 20.04+

시스템 의존성:
  macOS: brew install poppler
  Ubuntu: apt-get install poppler-utils
  Windows: poppler 바이너리 다운로드 필요

선택사항:
  - GPU (대용량 PDF 처리 시 권장)
  - 인터넷 연결 (AI API 호출용)
```

---

## 4. UI/UX 디자인: Neo-brutalism 스타일 ⭐ NEW

### 4.1 디자인 철학

Neo-brutalism (네오 부루탈리즘)은 웹 디자인의 새로운 트렌드로, 다음 특징을 가집니다:

- **굵은 테두리 (Bold Borders)**: 검은색 3-5px 두께
- **하드 섀도우 (Hard Shadows)**: offset 방식, 블러 없음
- **대비되는 색상 (High Contrast)**: 밝고 강렬한 색상 조합
- **심플한 형태 (Simple Shapes)**: 직사각형, 기본 도형
- **의도적인 "거친" 느낌**: 과도한 정제 없이 raw한 느낌

### 4.2 컬러 팔레트

```css
/* Neo-brutalism 메인 색상 */
:root {
  /* 배경 */
  --bg-primary: #FEF3E2;     /* 크림색 배경 */
  --bg-secondary: #FFFFFF;    /* 순백색 카드 */

  /* 강조색 */
  --accent-yellow: #FFE135;   /* 밝은 노랑 */
  --accent-blue: #4A90D9;     /* 파스텔 블루 */
  --accent-pink: #FF6B9D;     /* 밝은 핑크 */
  --accent-green: #7ED957;    /* 라임 그린 */
  --accent-orange: #FF9F43;   /* 오렌지 */

  /* 텍스트 */
  --text-primary: #1A1A2E;    /* 거의 검정 */
  --text-secondary: #4A4A68;  /* 진한 회색 */

  /* 테두리 */
  --border-color: #1A1A2E;    /* 검은 테두리 */
  --border-width: 3px;

  /* 그림자 */
  --shadow-offset: 5px;
  --shadow-color: #1A1A2E;
}
```

### 4.3 UI 컴포넌트 스타일

```css
/* Neo-brutalism 버튼 */
.neo-button {
  background: var(--accent-yellow);
  border: var(--border-width) solid var(--border-color);
  box-shadow: var(--shadow-offset) var(--shadow-offset) 0 var(--shadow-color);
  padding: 12px 24px;
  font-weight: 700;
  text-transform: uppercase;
  cursor: pointer;
  transition: transform 0.1s, box-shadow 0.1s;
}

.neo-button:hover {
  transform: translate(2px, 2px);
  box-shadow: 3px 3px 0 var(--shadow-color);
}

.neo-button:active {
  transform: translate(5px, 5px);
  box-shadow: none;
}

/* Neo-brutalism 카드 */
.neo-card {
  background: var(--bg-secondary);
  border: var(--border-width) solid var(--border-color);
  box-shadow: 8px 8px 0 var(--shadow-color);
  padding: 24px;
}

/* Neo-brutalism 입력 필드 */
.neo-input {
  border: var(--border-width) solid var(--border-color);
  padding: 12px 16px;
  font-size: 16px;
  background: white;
}

.neo-input:focus {
  outline: none;
  box-shadow: 4px 4px 0 var(--accent-blue);
}
```

### 4.4 메인 UI 레이아웃

```
┌─────────────────────────────────────────────────────────────────────┐
│ ╔═══════════════════════════════════════════════════════════════╗   │
│ ║  🎨 NotebookLM → PPTX Converter                               ║   │
│ ║  ─────────────────────────────────────────────────────────    ║   │
│ ║  by 배움의 달인                          [YouTube] [X/Twitter] ║   │
│ ╚═══════════════════════════════════════════════════════════════╝   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ ┌─────────────────────────────────────────────────────────┐ │    │
│  │ │                                                          │ │    │
│  │ │           📁 PDF 파일을 여기에 드래그                    │ │    │
│  │ │              또는 클릭하여 선택                           │ │    │
│  │ │                                                          │ │    │
│  │ │                  [파일 선택] 버튼                         │ │    │
│  │ │                                                          │ │    │
│  │ └─────────────────────────────────────────────────────────┘ │    │
│  │                        ↳ 드롭존 (점선 테두리)                │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ 📚 컨텍스트 자료 (선택사항)                                   │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │ ┌─────────────────────────────────────────────────────────┐ │    │
│  │ │                                                          │ │    │
│  │ │      📄 관련 자료를 여기에 드래그 (PDF, TXT, MD)          │ │    │
│  │ │           또는 클릭하여 선택                              │ │    │
│  │ │                                                          │ │    │
│  │ │        ✅ context_doc.pdf (2.1MB)                        │ │    │
│  │ │        ✅ notes.txt (12KB)                               │ │    │
│  │ │                                                          │ │    │
│  │ └─────────────────────────────────────────────────────────┘ │    │
│  │         ↳ AI가 발표자 노트 생성 시 참고할 맥락 자료          │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ ⚙️ 설정                                                     │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │                                                              │    │
│  │  AI 제공자: [● Gemini ○ OpenAI ○ Claude ○ Grok]            │    │
│  │                                                              │    │
│  │  모델명: [gemini-2.5-flash          ▼]                      │    │
│  │                                                              │    │
│  │  노트 스타일: [○ 간략 ● 상세 ○ 스크립트]                    │    │
│  │                                                              │    │
│  │  언어: [한국어 ▼]                                            │    │
│  │                                                              │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                                                              │    │
│  │   ████████████████████████░░░░░░░░  75%  3/4 슬라이드       │    │
│  │                                                              │    │
│  │              [🚀 변환 시작]  [📥 다운로드]                   │    │
│  │                                                              │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ 📋 미리보기                                                  │    │
│  ├─────────────────────────────────────────────────────────────┤    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐               │    │
│  │  │ Slide 1│ │ Slide 2│ │ Slide 3│ │ Slide 4│               │    │
│  │  │  [img] │ │  [img] │ │  [img] │ │  [img] │               │    │
│  │  └────────┘ └────────┘ └────────┘ └────────┘               │    │
│  │                                                              │    │
│  │  현재 선택: Slide 1                                          │    │
│  │  ─────────────────────────────────────────────              │    │
│  │  📝 발표자 노트:                                             │    │
│  │  "이 슬라이드에서는 Notion과 Obsidian의 핵심 차이점을..."    │    │
│  │                                                              │    │
│  └─────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.5 개발자 크레딧 및 소셜 링크

```python
DEVELOPER_INFO = {
    "name": "배움의 달인",
    "youtube": "https://www.youtube.com/@%EB%B0%B0%EC%9B%80%EC%9D%98%EB%8B%AC%EC%9D%B8-p5v",
    "twitter_x": "https://x.com/reallygood83"
}

# UI에 표시되는 버튼
# ┌─────────────────────┐    ┌─────────────────────┐
# │ 📺 YouTube          │    │ 𝕏 Twitter/X        │
# │ @배움의달인          │    │ @reallygood83       │
# └─────────────────────┘    └─────────────────────┘
```

---

## 5. 멀티 AI API 지원 ⭐ NEW

### 5.1 지원 AI 모델 (2025년 12월 기준 최신)

| Provider | 모델명 | 특징 | 기본값 |
|----------|--------|------|--------|
| **Google Gemini** | `gemini-2.5-flash` | 빠른 속도, 저렴, Vision 지원 | ✅ 기본 |
| | `gemini-2.5-pro` | 고품질, Vision 지원 | |
| | `gemini-3-flash` | 최신 (2025.12), 차세대 | |
| **OpenAI** | `gpt-4.1` | 코딩/분석 최적화 | |
| | `gpt-4.1-mini` | 빠르고 저렴 | |
| | `gpt-5.2` | 최신 플래그십 (2025.12) | |
| **Anthropic** | `claude-sonnet-4-5` | 균형잡힌 성능 | ✅ 기본 |
| | `claude-opus-4-5` | 최고 품질 | |
| | `claude-haiku-4-5` | 가장 빠름, 저렴 | |
| **xAI Grok** | `grok-4-1-fast` | 초고속, 2M 컨텍스트 | ✅ 기본 |
| | `grok-4.1` | 표준 품질 | |

### 5.2 API 설정 구조

```yaml
# config.yaml
ai_providers:
  # 활성화할 기본 제공자
  default: "gemini"

  gemini:
    api_key: "YOUR_GEMINI_API_KEY"
    model: "gemini-2.5-flash"  # 사용자 수동 변경 가능
    # 대안 모델: gemini-2.5-pro, gemini-3-flash

  openai:
    api_key: "YOUR_OPENAI_API_KEY"
    model: "gpt-4.1"  # 사용자 수동 변경 가능
    # 대안 모델: gpt-4.1-mini, gpt-5.2

  anthropic:
    api_key: "YOUR_ANTHROPIC_API_KEY"
    model: "claude-sonnet-4-5"  # 사용자 수동 변경 가능
    # 대안 모델: claude-opus-4-5, claude-haiku-4-5

  grok:
    api_key: "YOUR_XAI_API_KEY"  # xAI API 키
    model: "grok-4-1-fast"  # 사용자 수동 변경 가능
    # 대안 모델: grok-4.1
    # OpenRouter 경유 가능: 무료 티어 제공

# 발표자 노트 설정
speaker_notes:
  style: "detailed"  # brief, detailed, script
  language: "ko"     # ko, en, ja, zh
  max_length: 500    # 최대 글자수

# 컨텍스트 자료 설정 (선택사항)
context_materials:
  enabled: true                    # 컨텍스트 자료 사용 여부
  max_file_size_mb: 10             # 단일 파일 최대 크기 (MB)
  max_total_size_mb: 50            # 전체 파일 최대 크기 (MB)
  supported_formats:               # 지원 파일 형식
    - ".pdf"
    - ".txt"
    - ".md"
  chunk_size: 2000                 # 텍스트 청킹 크기 (토큰)
  include_in_prompt: true          # AI 프롬프트에 컨텍스트 포함
```

### 5.3 AI Provider 추상화

```python
# src/ai_providers/base.py
from abc import ABC, abstractmethod
from typing import Optional
from PIL import Image

class AIProvider(ABC):
    """AI 제공자 기본 추상 클래스"""

    def __init__(self, api_key: str, model: str):
        self.api_key = api_key
        self.model = model

    @abstractmethod
    def analyze_slide(self, image: Image.Image) -> str:
        """슬라이드 이미지 분석 및 발표자 노트 생성"""
        pass

    @abstractmethod
    def get_available_models(self) -> list[str]:
        """사용 가능한 모델 목록 반환"""
        pass


# src/ai_providers/gemini.py
import google.generativeai as genai

class GeminiProvider(AIProvider):
    """Google Gemini API 제공자"""

    MODELS = [
        "gemini-2.5-flash",
        "gemini-2.5-pro",
        "gemini-3-flash",
    ]

    def __init__(self, api_key: str, model: str = "gemini-2.5-flash"):
        super().__init__(api_key, model)
        genai.configure(api_key=api_key)
        self.client = genai.GenerativeModel(model)

    def analyze_slide(self, image: Image.Image) -> str:
        response = self.client.generate_content([
            SPEAKER_NOTES_PROMPT,
            image
        ])
        return response.text

    def get_available_models(self) -> list[str]:
        return self.MODELS


# src/ai_providers/grok.py
import openai  # OpenRouter 경유 시 동일 SDK 사용

class GrokProvider(AIProvider):
    """xAI Grok API 제공자"""

    MODELS = [
        "grok-4-1-fast",  # 2M context, 빠른 속도
        "grok-4.1",       # 표준 품질
    ]

    def __init__(self, api_key: str, model: str = "grok-4-1-fast"):
        super().__init__(api_key, model)
        # xAI 직접 또는 OpenRouter 경유
        self.client = openai.OpenAI(
            api_key=api_key,
            base_url="https://api.x.ai/v1"  # xAI 직접 연결
            # 또는 OpenRouter: "https://openrouter.ai/api/v1"
        )

    def analyze_slide(self, image: Image.Image) -> str:
        import base64
        import io

        # 이미지를 base64로 인코딩
        buffer = io.BytesIO()
        image.save(buffer, format='PNG')
        image_b64 = base64.b64encode(buffer.getvalue()).decode()

        response = self.client.chat.completions.create(
            model=self.model,
            messages=[
                {
                    "role": "user",
                    "content": [
                        {"type": "text", "text": SPEAKER_NOTES_PROMPT},
                        {"type": "image_url", "image_url": {"url": f"data:image/png;base64,{image_b64}"}}
                    ]
                }
            ]
        )
        return response.choices[0].message.content
```

### 5.4 모델 선택 가이드

```
┌─────────────────────────────────────────────────────────────────────┐
│  🎯 사용 목적별 권장 모델                                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  💰 비용 최적화:                                                     │
│  └─→ gemini-2.5-flash (가장 저렴, 충분한 품질)                       │
│  └─→ grok-4-1-fast (OpenRouter 무료 티어 가능)                      │
│  └─→ claude-haiku-4-5 (빠르고 저렴)                                 │
│                                                                      │
│  ⚡ 속도 최적화:                                                     │
│  └─→ grok-4-1-fast (2M 컨텍스트, 초고속)                            │
│  └─→ gemini-2.5-flash                                               │
│                                                                      │
│  📊 품질 최적화:                                                     │
│  └─→ gpt-5.2 (최신 OpenAI 플래그십)                                 │
│  └─→ claude-opus-4-5 (최고 분석력)                                  │
│  └─→ gemini-2.5-pro                                                 │
│                                                                      │
│  🇰🇷 한국어 최적화:                                                  │
│  └─→ claude-sonnet-4-5 (우수한 한국어 지원)                         │
│  └─→ gemini-2.5-flash (다국어 지원)                                 │
│                                                                      │
│  🆓 무료 사용 (테스트용):                                            │
│  └─→ grok-4-1-fast via OpenRouter (무료 티어)                       │
│  └─→ gemini-2.5-flash (무료 할당량 제공)                            │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6. AI 발표자 노트 생성 시스템

### 6.1 발표자 노트(스피커 노트)란?

PowerPoint의 발표자 노트는 **각 슬라이드별로 개별 첨부**됩니다:
- 각 슬라이드 하단에 별도의 노트 섹션 존재
- 발표자 보기(Presenter View)에서 청중에게는 보이지 않고 발표자만 볼 수 있음
- 발표 스크립트, 핵심 포인트, 추가 설명 등을 기록

```
┌──────────────────────────────────┐
│         슬라이드 1               │
│    [NotebookLM 생성 이미지]      │
├──────────────────────────────────┤
│ 📝 발표자 노트 (Slide 1)         │
│ - 이 슬라이드에서 강조할 포인트   │
│ - AI가 분석한 핵심 내용 요약      │
│ - 청중에게 전달할 메시지          │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│         슬라이드 2               │
│    [NotebookLM 생성 이미지]      │
├──────────────────────────────────┤
│ 📝 발표자 노트 (Slide 2)         │
│ - 슬라이드 2의 별도 노트          │
│ - 각 슬라이드마다 독립적          │
└──────────────────────────────────┘
```

### 6.2 AI 분석 워크플로우

```
┌─────────────────────────────────────────────────────────┐
│  Step 1: PDF 페이지 이미지 추출                          │
│  └─→ pdf2image / PyMuPDF로 고해상도 이미지 변환          │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Step 2: 이미지 크기 조정 (Stretch to 16:9)             │
│  └─→ 1376×768 → 1920×1080 (0.78% 비율 조정)            │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Step 2.5: 컨텍스트 자료 처리 (선택사항)                  │
│  └─→ PDF/TXT/MD 파일 텍스트 추출                        │
│  └─→ 토큰 제한에 맞게 청킹 (chunk_size: 2000)           │
│  └─→ 관련 맥락 정보로 AI 프롬프트 강화                   │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Step 3: AI Vision 분석 (각 슬라이드별)                  │
│  └─→ Gemini / GPT / Claude / Grok Vision               │
│  └─→ 슬라이드 내용 분석 및 요약                          │
│  └─→ 컨텍스트 자료 참조하여 발표자 노트 품질 향상         │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Step 4: 발표자 노트 생성                                │
│  └─→ AI가 분석 결과를 바탕으로 발표 스크립트 작성         │
│  └─→ 핵심 포인트, 전환 멘트, 추가 설명 포함               │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Step 5: PPTX 생성                                       │
│  └─→ python-pptx로 슬라이드 생성                         │
│  └─→ 이미지 삽입 + 발표자 노트 첨부                       │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  Step 6: 다운로드                                        │
│  └─→ 완성된 .pptx 파일 제공                              │
└─────────────────────────────────────────────────────────┘
```

### 6.3 AI 프롬프트 설계

```python
SPEAKER_NOTES_PROMPT = """
이 슬라이드 이미지를 분석하고 발표자 노트를 작성해주세요.

발표자 노트에 포함할 내용:
1. **핵심 메시지**: 이 슬라이드에서 전달해야 할 가장 중요한 포인트
2. **상세 설명**: 슬라이드에 표시된 내용의 부연 설명
3. **전환 멘트**: 다음 슬라이드로 넘어가기 위한 자연스러운 연결 문구
4. **예상 질문**: 청중이 할 수 있는 질문과 답변 가이드
5. **발표 팁**: 강조할 부분, 속도 조절 등

형식:
- 한국어로 작성
- 2-3분 분량의 발표 스크립트
- 읽기 쉽게 bullet point 활용
"""
```

---

## 7. 구현 코드

### 7.1 메인 변환 로직

```python
# src/converter.py
from pdf2image import convert_from_path
from pptx import Presentation
from pptx.util import Inches
import yaml
import io
from pathlib import Path
from typing import Optional

from ai_providers import GeminiProvider, OpenAIProvider, AnthropicProvider, GrokProvider

# PowerPoint 16:9 표준 크기
SLIDE_WIDTH = Inches(13.333)
SLIDE_HEIGHT = Inches(7.5)

class NotebookLMToPPTX:
    """NotebookLM PDF를 AI 발표자 노트가 포함된 PPTX로 변환"""

    def __init__(self, config_path: str = "config.yaml"):
        with open(config_path, 'r') as f:
            self.config = yaml.safe_load(f)

        self.ai_provider = self._init_ai_provider()

    def _init_ai_provider(self):
        """설정에 따라 AI 제공자 초기화"""
        provider_name = self.config['ai_providers']['default']
        provider_config = self.config['ai_providers'][provider_name]

        providers = {
            'gemini': GeminiProvider,
            'openai': OpenAIProvider,
            'anthropic': AnthropicProvider,
            'grok': GrokProvider,
        }

        return providers[provider_name](
            api_key=provider_config['api_key'],
            model=provider_config['model']
        )

    def pdf_to_images(self, pdf_path: str, dpi: int = 300):
        """PDF를 고해상도 이미지로 변환"""
        images = convert_from_path(pdf_path, dpi=dpi)
        return images

    def create_pptx(
        self,
        pdf_path: str,
        output_path: str,
        note_style: str = "detailed",
        language: str = "ko",
        context_materials: Optional[list[str]] = None,  # 컨텍스트 자료 경로 리스트
        progress_callback: Optional[callable] = None
    ):
        """메인 변환 함수"""
        # 1. PDF를 이미지로 변환
        images = self.pdf_to_images(pdf_path)
        total_slides = len(images)
        print(f"📄 {total_slides}개 슬라이드 발견")

        # 2. 컨텍스트 자료 처리 (선택사항)
        context_text = None
        if context_materials and self.config.get('context_materials', {}).get('enabled', True):
            context_text = self._process_context_materials(context_materials)
            print(f"📚 컨텍스트 자료 {len(context_materials)}개 로드됨")

        # 3. 프레젠테이션 생성
        prs = Presentation()
        prs.slide_width = SLIDE_WIDTH
        prs.slide_height = SLIDE_HEIGHT
        blank_layout = prs.slide_layouts[6]  # 빈 레이아웃

        # 4. 각 슬라이드 처리
        for i, image in enumerate(images):
            if progress_callback:
                progress_callback(i + 1, total_slides)

            print(f"🔄 슬라이드 {i+1}/{total_slides} 처리 중...")

            # 이미지 임시 저장
            temp_path = f'/tmp/slide_{i}.png'
            image.save(temp_path, 'PNG')

            # 슬라이드 추가
            slide = prs.slides.add_slide(blank_layout)

            # 이미지 삽입 (STRETCH - 0.78% 비율 차이 무시)
            slide.shapes.add_picture(
                temp_path,
                left=Inches(0),
                top=Inches(0),
                width=SLIDE_WIDTH,
                height=SLIDE_HEIGHT
            )

            # AI 발표자 노트 생성 (컨텍스트 자료 참조)
            notes = self.ai_provider.analyze_slide(image, context=context_text)

            # 발표자 노트 삽입
            notes_slide = slide.notes_slide
            notes_slide.notes_text_frame.text = notes

            print(f"✅ 슬라이드 {i+1} 완료")

        # 4. 저장
        prs.save(output_path)
        print(f"🎉 변환 완료: {output_path}")
        return output_path
```

### 7.2 python-pptx 발표자 노트 검증 코드

```python
from pptx import Presentation
from pptx.util import Inches

def verify_speaker_notes():
    """발표자 노트 기능 검증"""
    prs = Presentation()
    prs.slide_width = Inches(13.333)
    prs.slide_height = Inches(7.5)

    blank_layout = prs.slide_layouts[6]
    slide = prs.slides.add_slide(blank_layout)

    # ⭐ 발표자 노트 추가 - 핵심 코드
    notes_slide = slide.notes_slide
    notes_text_frame = notes_slide.notes_text_frame

    ai_generated_notes = """
    📌 핵심 메시지:
    이 슬라이드에서는 Notion과 Obsidian의 핵심 차이점을 설명합니다.

    💡 발표 포인트:
    1. Notion은 클라우드 기반, Obsidian은 로컬 파일 기반
    2. 협업 vs 개인 지식 관리의 차이
    3. 각 도구의 적합한 사용 시나리오

    🔄 전환 멘트:
    "이제 각 도구의 구체적인 기능을 살펴보겠습니다."

    ⏱️ 예상 소요 시간: 2분
    """

    notes_text_frame.text = ai_generated_notes
    prs.save('test_with_notes.pptx')
    print("✅ 발표자 노트 테스트 완료")

verify_speaker_notes()
```

---

## 8. 핵심 기능 정의

### 8.1 MVP 기능 (Must Have)

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| PDF 업로드 | 드래그앤드롭 또는 파일 선택 | P0 |
| 슬라이드 변환 | PDF → PPTX (이미지 STRETCH 방식) | P0 |
| **🤖 AI 발표자 노트** | **각 슬라이드별 AI 분석 및 노트 자동 생성** | **P0** ⭐ |
| **멀티 AI API** | **Gemini/OpenAI/Claude/Grok 선택** | **P0** ⭐ |
| **📚 컨텍스트 자료** | **PDF/TXT/MD 맥락 자료로 발표자 노트 품질 향상** | **P0** ⭐ |
| 16:9 비율 유지 | PPT 표준 와이드스크린 | P0 |
| 다운로드 | .pptx 파일 저장 | P0 |
| Neo-brutalism UI | 모던 UI/UX 디자인 | P1 |
| 한국어 UI | 한국어 인터페이스 | P1 |

### 8.2 Phase 2 기능 (Nice to Have)

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 노트 스타일 선택 | 간략/상세/스크립트 형태 선택 | P2 |
| 워터마크 제거 옵션 | NotebookLM 로고 제거 | P2 |
| 노트 편집 | 생성된 노트 수정 인터페이스 | P2 |
| 배치 변환 | 여러 PDF 동시 처리 | P2 |

---

## 9. 기술 스택 (Updated)

### 9.1 Core 라이브러리

```python
# PDF 처리
pdf2image          # PDF → 이미지 변환 (poppler 기반)
PyMuPDF (fitz)     # 대안: 빠른 PDF 렌더링

# PPTX 생성
python-pptx        # .pptx 파일 생성/조작

# 이미지 처리
Pillow             # 이미지 리사이즈, 품질 조정

# AI 통합 (멀티 API)
google-generativeai # Gemini API
openai             # OpenAI API (GPT + Grok via OpenRouter)
anthropic          # Claude API

# GUI
tkinter            # 기본 GUI (내장)
customtkinter      # 모던 UI 컴포넌트

# 설정 관리
pyyaml             # YAML 설정 파일
```

### 9.2 시스템 의존성

```bash
# macOS
brew install poppler

# Ubuntu/Debian
apt-get install poppler-utils

# Windows
# poppler 바이너리 설치 필요
# https://github.com/oschwartz10612/poppler-windows/releases
```

### 9.3 requirements.txt

```
# NotebookLM PDF to PPTX Converter
# by 배움의 달인

# PDF Processing
pdf2image>=1.16.3

# PowerPoint Generation
python-pptx>=0.6.21

# Image Processing
Pillow>=10.0.0

# AI APIs (Multi-provider support)
google-generativeai>=0.8.0
openai>=1.50.0
anthropic>=0.40.0

# Configuration
pyyaml>=6.0

# GUI (Optional - for desktop app)
customtkinter>=5.2.0

# Progress bar
tqdm>=4.65.0
```

---

## 10. 개발 로드맵

### 10.1 Phase 1: MVP (1-2주)

- [x] 스펙 문서 작성
- [x] 크기 분석 (NotebookLM vs PPT)
- [x] 배포 방식 결정 (GitHub)
- [x] UI/UX 디자인 결정 (Neo-brutalism)
- [x] AI 모델 조사 (최신 2025년 모델)
- [ ] 기본 변환 로직 구현
- [ ] 멀티 AI API 통합
- [ ] CLI 인터페이스 개발
- [ ] 테스트 및 버그 수정

### 10.2 Phase 2: GUI (2-3주)

- [ ] Neo-brutalism GUI 개발
- [ ] 드래그앤드롭 업로드
- [ ] 실시간 미리보기
- [ ] 진행 상황 표시
- [ ] 설정 UI

### 10.3 Phase 3: 배포 (1주)

- [ ] GitHub 레포지토리 생성
- [ ] README 작성
- [ ] 설치 가이드 작성
- [ ] 릴리즈 버전 태깅

---

## 11. 구현 가능성 결론

| 항목 | 상태 | 설명 |
|------|------|------|
| PDF → 이미지 변환 | ✅ 검증됨 | pdf2image, PyMuPDF 모두 가능 |
| 크기 차이 해결 | ✅ 검증됨 | STRETCH 방식, 0.78% 차이 무시 가능 |
| PPTX 생성 | ✅ 검증됨 | python-pptx 완벽 지원 |
| 발표자 노트 삽입 | ✅ 검증됨 | `slide.notes_slide.notes_text_frame.text` |
| 멀티 AI Vision | ✅ 검증됨 | Gemini, GPT, Claude, Grok 모두 지원 |
| **컨텍스트 자료 처리** | ✅ 검증됨 | PDF/TXT/MD 파일 로드 및 토큰 청킹 지원 |
| 16:9 비율 | ✅ 검증됨 | Inches(13.333) x Inches(7.5) |
| Neo-brutalism UI | ✅ 가능 | customtkinter로 구현 가능 |

**결론: 100% 구현 가능** 🎉

---

## 부록

### A. 개발자 정보

| 항목 | 내용 |
|------|------|
| **개발자** | 배움의 달인 |
| **YouTube** | [@배움의달인](https://www.youtube.com/@%EB%B0%B0%EC%9B%80%EC%9D%98%EB%8B%AC%EC%9D%B8-p5v) |
| **X (Twitter)** | [@reallygood83](https://x.com/reallygood83) |

### B. 샘플 PDF 분석 결과

**파일**: `Notion_versus_Obsidian_Two_Worlds.pdf`
- 페이지 수: 10
- 파일 크기: 8.1MB
- **정확한 크기**: 1376 × 768 픽셀
- **정확한 비율**: 1.7917:1 (~16.1:9)
- 콘텐츠: Notion vs Obsidian 비교 가이드
- 언어: 한국어
- 디자인: 크림색 배경, 아이콘/일러스트 포함

### C. PPT 크기 표준 참조

| 형식 | 크기 (인치) | 크기 (픽셀 @144 DPI) | 비율 |
|------|------------|---------------------|------|
| 16:9 (와이드) | 13.333 × 7.5 | 1920 × 1080 | 1.7778 |
| 4:3 (표준) | 10 × 7.5 | 1440 × 1080 | 1.3333 |
| NotebookLM | - | 1376 × 768 | 1.7917 |

### D. 참고 자료

- [python-pptx 공식 문서](https://python-pptx.readthedocs.io/)
- [python-pptx 발표자 노트 가이드](https://python-pptx.readthedocs.io/en/latest/user/notes.html)
- [pdf2image GitHub](https://github.com/Belval/pdf2image)
- [NotebookLM 공식 사이트](https://notebooklm.google/)
- [Google Gemini API](https://ai.google.dev/)
- [OpenAI GPT API](https://platform.openai.com/)
- [Anthropic Claude API](https://docs.anthropic.com/)
- [xAI Grok API](https://x.ai/api)

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| v0.1 | 2025-12-25 | 초기 스펙 문서 작성 |
| v0.2 | 2025-12-25 | AI 발표자 노트 생성 기능 추가, 구현 검증 코드 포함 |
| v0.3 | 2025-12-25 | **대규모 업데이트** |
| | | - 크기 분석 추가 (1376×768 vs 1920×1080, 0.78% 차이) |
| | | - GitHub 배포 방식 결정 및 프로젝트 구조 정의 |
| | | - Neo-brutalism UI/UX 디자인 스펙 추가 |
| | | - 개발자 크레딧 (배움의 달인) 및 소셜 링크 추가 |
| | | - 멀티 AI API 지원 (Gemini, OpenAI, Anthropic, Grok) |
| | | - 2025년 12월 기준 최신 모델 정보 반영 |
| v0.4 | 2025-12-25 | **📚 컨텍스트 자료 기능 추가** |
| | | - 발표자 노트 품질 향상을 위한 맥락 자료 첨부 기능 |
| | | - PDF/TXT/MD 파일 지원 (Section 4.4 UI, Section 5.2 config) |
| | | - 토큰 청킹 (2000 토큰 단위) 및 스마트 참조 |
| | | - AI Workflow Step 2.5 컨텍스트 처리 추가 (Section 6.2) |
| | | - converter.py에 context_materials 파라미터 추가 (Section 7.1) |
| | | - MVP 기능표에 P0 우선순위 반영 (Section 8.1) |

---

*이 문서는 대화를 통해 지속적으로 업데이트됩니다.*
